#include <stdio.h>

/**
 * main - causes an infinite loop
 * @\\o/\n: part of a printed line and newline character to show the
 * loop has been avoided
 * Return: 0
 */

int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");
/**
 * while - giving an infinte loop
 * i = 0;
 * while (i < 10)
 * {
 *	putchar(i);
 * }
 */
	printf("Infinite loop avoided! \\o/\n");
	return (0);
}
