#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the name of the function
 * @argc: count of arguments
 * @argv: the array of arguments
 * Return: Always 0.
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
